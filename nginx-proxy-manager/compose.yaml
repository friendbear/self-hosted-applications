version: '3'
services:
  app:
    image: 'docker.io/jc21/nginx-proxy-manager:latest'
    restart: unless-stopped
    ports:
      - '10080:80'
      - '10081:81'
      - '10443:443'
    volumes:
      - ./data:/data
      - ./letsencrypt:/etc/letsencrypt

